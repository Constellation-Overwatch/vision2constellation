# Required: Organization UUID (obtain from Overwatch Edge Awareness Kit UI or DBA)
CONSTELLATION_ORG_ID=

# Required: Entity UUID (obtain from Overwatch Edge Awareness Kit UI or DBA)
# Must be valid for NATS KV keys: alphanumeric, hyphens, underscores only (no dots, spaces, asterisks)
CONSTELLATION_ENTITY_ID=

# Smart Publishing Thresholds
# Control when detection events are published (reduces bandwidth & improves signal-to-noise)

# Movement threshold: Publish when object moves > N% of frame (default: 0.05 = 5%)
# Lower = more updates, Higher = fewer updates
SIGINT_MOVEMENT_THRESHOLD=0.05

# Confidence threshold: Publish when confidence changes > N (default: 0.1 = 10%)
# Lower = more updates, Higher = fewer updates
SIGINT_CONFIDENCE_THRESHOLD=0.1

# NATS/JetStream Configuration  
# NATS server URL (default: localhost for development)
NATS_URL=nats://localhost:4222

# NATS subject root for constellation events
NATS_SUBJECT_ROOT=constellation.events.isr

# JetStream stream name for constellation events
NATS_STREAM_NAME=CONSTELLATION_EVENTS

# Key-Value store name for global state
NATS_KV_STORE_NAME=CONSTELLATION_GLOBAL_STATE

# NATS Auth Token (required for production)
NATS_AUTH_TOKEN=reindustrialize-america

# Video Frame Streaming
# Enable streaming frames to Constellation Overwatch (default: false)
ENABLE_FRAME_STREAMING=false

# JPEG encoding quality (1-100, higher = better quality, larger size)
FRAME_JPEG_QUALITY=75

# Target FPS for frame streaming (lower than capture FPS to reduce bandwidth)
FRAME_TARGET_FPS=15

# Max dimension in pixels (scales down if frame exceeds this)
FRAME_MAX_DIMENSION=1280

# Include detection overlays in streamed frames
FRAME_INCLUDE_DETECTIONS=true

# Video stream name (separate from events stream)
NATS_VIDEO_STREAM_NAME=CONSTELLATION_VIDEO_FRAMES

# Video subject root
NATS_VIDEO_SUBJECT_ROOT=constellation.video

# HuggingFace Configuration
# Disable HuggingFace Hub connectivity checks (use cached models only)
HF_HUB_OFFLINE=0
TRANSFORMERS_OFFLINE=0